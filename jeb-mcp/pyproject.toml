[project]
name = "jeb-mcp"
version = "2.0.0"
description = "MCP server integration for JEB Decompiler (Official API)"
dependencies = [
    "fastmcp>=0.4.1",
]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"